# adapter-cli

Minimal example showing AgentTK adapter contracts, capability checks, and normalized adapter failures.

```bash
node examples/adapter-cli/index.mjs status --json
node examples/adapter-cli/index.mjs create-task
```
